# Repository handoff

- Keep email support minimal: the existing `POST /api/quote` handler uses runtime-only `RESEND_API_KEY` and `RESEND_FROM_EMAIL`. It sends to the fixed recipient `Piechotagrpinc@gmail.com` and sets Reply-To to the visitor's email. Never expose the API key through browser code or `NEXT_PUBLIC_` variables.
- Self-hosted deployments must run the Next.js server. Use the standalone output plus `public/` and `.next/static/`; a static `out/` export cannot run the email endpoint. Preserve the identity-bearing `GET /health` response.
- On 2026-09-23, the owner authorized recovery from the legacy static-export recipe failure. The authoritative host project catalog now packages `.next/standalone` behind the existing launcher, and the dashboard controller deployed `efcba328c82ecde775bef16fed659d41c7f65529` successfully. Keep the obsolete `prepare-export.mjs` step out of the active recipe. The standalone image cache has a dedicated service-owned host directory bound into `.next/cache`; the application files stay read-only. Host configuration stays outside this repository. Public health, pages, assets, image optimization, and non-sending email validation passed; both Resend variables were present in the running process. Actual email delivery was not tested.
- The current dashboard editor preserves existing build/start recipes. Retrying a failed deployment or editing environment variables cannot change the runtime profile; a host operator must repair the effective project catalog recipe first, preserving its saved environment. Once repaired, ordinary dashboard deployment remains the supported path.
- Dashboard environment saves require deployment/restart to reach the running process. The sender domain must already be verified in Resend. The email API key is not needed during build.
- Run `npm run lint` and `npm test`. Email tests must mock the provider instead of sending messages.
